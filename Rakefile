#1.git pull origin source
#2.copy _site to backup
#3.git co master
#4.copy backup to master branch
#5.git pull origin master
task :puts_hello do
  puts "Summit new post to source banch"
  system 'git add'
  system 'cd'
  system 'mkdir world'
end